DATABASE_URL=postgresql+psycopg://USER:PASSWORD@localhost:5432/agent_governance

# Optional. Without a key the agent falls back to the deterministic rule-based decision layer.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
